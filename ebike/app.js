const LABELS={scenery:'Natur',trail:'Trail',low_traffic:'ruhig',emtb_fun:'E-MTB',viewpoints:'Aussicht',loop_quality:'Loop',surface_confidence:'Daten'};
const $=id=>document.getElementById(id),asset=p=>String(p||'').startsWith('http')?p:'/ebike/'+String(p||'').replace(/^\//,'');
let DATA=[],MODEL={},WEATHER=null,LOGS=[],FAVS=[],FOCUS=null;
const q=$('q'),sort=$('sort'),type=$('type'),timeBudget=$('timeBudget'),grid=$('grid'),summary=$('summary'),rail=$('routeRail'),focusEl=$('focusRoute'),batteryLeft=$('batteryLeft'),batteryLeftText=$('batteryLeftText'),assist=$('assist');
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function pct(n){return Math.max(0,Math.min(100,Math.round(n)))}function today(){return new Date().toISOString().slice(0,10)}
function condition(){if(!WEATHER)return {score:60,label:'lädt',cls:'warn',advice:'Wetterdaten werden geladen. Bis dahin neutral bewertet.',short:'lädt'};const rain=WEATHER.rain48,temp=WEATHER.temp,wind=WEATHER.wind;let score=100-rain*9-Math.max(0,wind-25)*1.3+(temp<4?-12:0);score=pct(score);if(score>78)return {score,label:'Sehr gut fahrbar',short:'sehr gut',cls:'good',advice:'Trocken genug für Wald/Trail – normale Rücksicht reicht.'};if(score>58)return {score,label:'Fahrbar mit Vorsicht',short:'fahrbar',cls:'warn',advice:'Waldpassagen prüfen, rutschige Wurzeln einkalkulieren.'};if(score>38)return {score,label:'Schotter bevorzugen',short:'schotter',cls:'warn',advice:'Trailabschnitte schonen; besser Weser/Forst/Schotter.'};return {score,label:'Trail lieber meiden',short:'meiden',cls:'bad',advice:'Zu nass/kalt/windig – Runde kurz halten oder verschieben.'}}
async function loadWeather(){try{const url='https://api.open-meteo.com/v1/forecast?latitude=52.1224&longitude=9.3448&hourly=rain&current=temperature_2m,wind_speed_10m&past_days=2&forecast_days=2&timezone=Europe%2FBerlin';const j=await fetch(url).then(r=>r.json());const rain=(j.hourly?.rain||[]).slice(0,48).reduce((a,b)=>a+(Number(b)||0),0);WEATHER={rain48:Math.round(rain*10)/10,temp:Math.round(j.current?.temperature_2m??0),wind:Math.round(j.current?.wind_speed_10m??0)};$('weatherPill').textContent=`${WEATHER.temp}°C · ${WEATHER.rain48}mm/48h · ${WEATHER.wind}km/h`}catch(e){WEATHER={rain48:0,temp:12,wind:10,offline:true};$('weatherPill').textContent='Wetter offline · neutral'}render()}
function durationMax(r){const nums=String(r.duration||'').replace(/,/g,'.').match(/\d+(?:\.\d+)?/g)||[];return nums.length?Number(nums[nums.length-1]):999}
function populate(){type.innerHTML='<option value="">Alle Typen</option>'+[...new Set(DATA.map(r=>r.type))].sort().map(t=>`<option>${esc(t)}</option>`).join('');$('logRoute').innerHTML=DATA.map(r=>`<option value="${esc(r.id)}">${esc(r.name)}</option>`).join('');$('logDate').value=today()}
function batteryStatus(r){const need=r.battery_model?.[assist.value]||50,left=Number(batteryLeft.value),reserve=r.battery_model?.reserve_percent||15,margin=left-need-reserve;return {need,left,margin,label:margin>=20?'locker':margin>=5?'passt':margin>=0?'dünn':'zu knapp',cls:margin>=20?'good':margin>=0?'warn':'bad'}}
function readiness(r){const b=batteryStatus(r),c=condition(),hours=durationMax(r),budget=Number(timeBudget.value),problems=[];if(b.margin<0)problems.push('Akku zu knapp');else if(b.margin<8)problems.push('Akku-Reserve dünn');if(c.score<40)problems.push('Trailzustand schlecht');else if(c.score<60&&r.scores.trail>74)problems.push('Trail nur vorsichtig');if(hours>budget)problems.push('Zeitfenster zu klein');let cls=problems.length?problems.some(p=>p.includes('zu knapp')||p.includes('schlecht'))?'bad':'warn':'good';return {cls,label:problems.length?problems.join(' · '):'bereit',problems}}
function smartScore(r){const c=condition(),b=batteryStatus(r),ready=readiness(r);let s=r.score+(c.score-60)*0.10+Math.min(10,Math.max(-24,b.margin*0.42))+(FAVS.includes(r.id)?2.5:0);if(durationMax(r)>Number(timeBudget.value))s-=18;if(ready.cls==='bad')s-=18;else if(ready.cls==='warn')s-=6;if(r.type.includes('Feierabend'))s+=2;return Math.round(s*10)/10}
function rows(){let rows=[...DATA];const query=q.value.toLowerCase();if(query)rows=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(query));if(type.value)rows=rows.filter(r=>r.type===type.value);const key=sort.value;rows.sort((a,b)=>key==='smart'?smartScore(b)-smartScore(a):key==='difficulty'?String(a[key]).localeCompare(String(b[key])):(b[key]||0)-(a[key]||0));return rows}
function statusPill(text,cls){return `<span class="pill ${cls||''}">${esc(text)}</span>`}
function satelliteMap(id){return `<div class="sat-map" data-sat="${esc(id)}"><div class="tile-layer"></div><svg class="sat-track" viewBox="0 0 100 62"><polyline points=""/></svg><div class="sat-badge">Satellit · Track</div></div>`}
function miniMap(id,cls='card-map'){return `<svg class="${cls}" viewBox="0 0 100 62" data-track="${esc(id)}"><polyline points=""/></svg>`}
function renderSummary(rs){const c=condition(),top=rs[0],fav=FAVS.length;summary.innerHTML=`<article><span>Top jetzt</span><b>${top?esc(top.name):'–'}</b></article><article class="${c.cls}"><span>Fahrbarkeit</span><b>${esc(c.label)}</b></article><article><span>Akku</span><b>${batteryLeft.value}% · ${esc(assist.value)}</b></article><article><span>Favoriten</span><b>${fav||'keine'}</b></article>`;$('conditionPanel').innerHTML=`<div><strong>Trailzustand: ${c.score}/100 · ${esc(c.label)}</strong><p>${esc(c.advice)} ${WEATHER?`Wetter: ${WEATHER.temp}°C, ${WEATHER.rain48} mm Regen/48h, ${WEATHER.wind} km/h Wind.`:''}</p></div><span class="condition-dot ${c.cls}"></span>`}
function renderRecommendation(rs){const r=rs[0];if(!r){$('recommendation').innerHTML='';return}const b=batteryStatus(r),ready=readiness(r);$('recommendation').innerHTML=`<div class="rec-top"><span>Empfehlung</span><b>${smartScore(r)}</b></div><h2>${esc(r.name)}</h2><p>${esc(r.best_for)}</p><div class="rec-stats"><span>${r.distance_km} km</span><span>${r.elevation_m} hm</span><span>${esc(r.duration)}</span><span class="${b.cls}">${b.need}% Akku · ${esc(b.label)}</span><span class="${ready.cls}">${esc(ready.label)}</span></div><div class="hero-actions"><a class="primary" href="#routes" data-focus="${esc(r.id)}">Fokus öffnen</a><a class="ghost" href="${esc(asset(r.gpx_file))}" download>GPX</a></div>`}
function railItem(r){const active=r.id===FOCUS?'active':'',fav=FAVS.includes(r.id)?'★':'☆',ready=readiness(r);return `<button class="rail-item ${active}" data-focus="${esc(r.id)}"><span><b>${esc(r.name)}</b><small>${r.distance_km} km · ${r.elevation_m} hm · ${esc(ready.label)}</small></span><em class="${ready.cls}">${smartScore(r)}</em><i data-fav="${esc(r.id)}">${fav}</i></button>`}
function renderRail(rs){$('routeCount').textContent=`${rs.length} Treffer`;rail.innerHTML=rs.map(railItem).join('')}
function gallery(r){const imgs=r.gallery||[];if(!imgs.length)return `<div class="empty-gallery">Noch keine passenden öffentlichen Bilder gefunden.</div>`;return `<div class="gallery">${imgs.map(i=>`<figure><img src="${esc(i.url)}" alt="${esc(i.title)}"><figcaption>${esc(i.title)} · ${esc(i.license)}</figcaption></figure>`).join('')}</div>`}
function renderFocus(rs){if(!FOCUS||!rs.some(r=>r.id===FOCUS))FOCUS=rs[0]?.id;const r=DATA.find(x=>x.id===FOCUS)||rs[0];if(!r){focusEl.innerHTML='<p>Keine Route gefunden.</p>';return}const b=batteryStatus(r),ready=readiness(r);focusEl.innerHTML=`<div class="focus-layout"><div><div class="focus-head"><div><p class="eyebrow">Fokusroute</p><h2>${esc(r.name)}</h2><p>${esc(r.region)} · ${esc(r.start_mode)}</p></div><button class="fav-btn" data-fav="${esc(r.id)}">${FAVS.includes(r.id)?'★ Favorit':'☆ Favorit'}</button></div>${satelliteMap(r.id)}<p class="route-desc">${esc(r.best_for)}</p><div class="tagrow">${(r.decision_tags||r.highlights).map(h=>statusPill(h)).join('')}</div>${gallery(r)}</div><aside class="focus-side"><div class="metric"><span>Smartscore</span><b>${smartScore(r)}</b></div><div class="metric ${ready.cls}"><span>Ready?</span><b>${esc(ready.label)}</b></div><div class="metric"><span>Distanz / Höhenmeter</span><b>${r.distance_km} km · ${r.elevation_m} hm</b></div><div class="metric"><span>Dauer</span><b>${esc(r.duration)}</b></div><div class="metric ${b.cls}"><span>Akku</span><b>${b.need}% · ${esc(b.label)}</b></div><div class="pack"><b>Mitnehmen</b><p>${esc(r.packing_hint||'Wasser, Tool, Pumpe, Licht.')}</p></div><div class="focus-actions"><a href="${esc(r.planner_link)}" target="_blank" rel="noopener">BRouter</a><a href="${esc(asset(r.gpx_file))}" download>GPX</a><a href="${esc(r.navigation_link)}" target="_blank" rel="noopener">Navigation</a></div></aside></div><details><summary>Risiken, Score und nächste Schritte</summary><p><b>Check:</b> ${esc(r.risk_notes.join(' · '))}</p><p><b>Track:</b> ${esc(r.track_source)} · <b>Untergrund:</b> ${esc(r.surface)}</p><p>${esc(r.photo_note||'')}</p><div class="scorebars">${Object.entries(r.scores).map(([k,v])=>`<div class="scorebar"><span>${LABELS[k]||k}</span><div class="track"><div class="fill w${pct(v)}"></div></div><b>${v}</b></div>`).join('')}</div></details>`;hydrateMaps();hydrateSatellite()}
function card(r){const b=batteryStatus(r),ready=readiness(r),hero=(r.gallery||[])[0];return `<article class="route-card"><div class="card-head"><h3>${esc(r.name)}</h3><button data-fav="${esc(r.id)}">${FAVS.includes(r.id)?'★':'☆'}</button></div>${hero?`<img class="card-photo" src="${esc(hero.url)}" alt="${esc(hero.title)}">`:miniMap(r.id)}<div class="mini-stats"><span>${r.distance_km} km</span><span>${r.elevation_m} hm</span><span class="${b.cls}">${b.need}% Akku</span><span class="${ready.cls}">${esc(ready.label)}</span></div><p>${esc(r.best_for)}</p><button class="open-focus" data-focus="${esc(r.id)}">Details</button></article>`}
function renderCards(rs){grid.innerHTML=rs.map(card).join('')}
function renderCompare(rs){$('compareTable').innerHTML=`<table><thead><tr><th>Route</th><th>Score</th><th>Zeit</th><th>km / hm</th><th>Akku</th><th>Status</th><th>Aktion</th></tr></thead><tbody>${rs.map(r=>{const b=batteryStatus(r),ready=readiness(r);return `<tr><td><strong>${esc(r.name)}</strong><small>${esc(r.type)}</small></td><td>${smartScore(r)}</td><td>${esc(r.duration)}</td><td>${r.distance_km} km<br>${r.elevation_m} hm</td><td class="${b.cls}">${b.need}% · ${esc(b.label)}</td><td class="${ready.cls}">${esc(ready.label)}</td><td><a href="${esc(asset(r.gpx_file))}" download>GPX</a></td></tr>`}).join('')}</tbody></table>`}
function render(){batteryLeftText.textContent=batteryLeft.value+'%';const rs=rows();renderSummary(rs);renderRecommendation(rs);renderRail(rs);renderFocus(rs);renderCards(rs);renderCompare(rs);hydrateMaps()}
async function hydrateMaps(){for(const svg of document.querySelectorAll('svg[data-track]')){if(svg.dataset.ready)return;const r=DATA.find(x=>x.id===svg.dataset.track);if(!r?.track_geojson_file)continue;try{const gj=await fetch(asset(r.track_geojson_file)).then(x=>x.json());const pts=gj.features[0].geometry.coordinates.map(c=>[c[0],c[1]]),xs=pts.map(p=>p[0]),ys=pts.map(p=>p[1]),minx=Math.min(...xs),maxx=Math.max(...xs),miny=Math.min(...ys),maxy=Math.max(...ys);svg.querySelector('polyline').setAttribute('points',pts.map(([x,y])=>`${((x-minx)/(maxx-minx||1)*96+2).toFixed(1)},${(60-((y-miny)/(maxy-miny||1)*58)).toFixed(1)}`).join(' '));svg.dataset.ready='1'}catch(e){}}}
function lon2tile(lon,z){return Math.floor((lon+180)/360*Math.pow(2,z))}function lat2tile(lat,z){const r=lat*Math.PI/180;return Math.floor((1-Math.log(Math.tan(r)+1/Math.cos(r))/Math.PI)/2*Math.pow(2,z))}function project(lon,lat,z){const n=Math.pow(2,z),x=(lon+180)/360*n*256,r=lat*Math.PI/180,y=(1-Math.log(Math.tan(r)+1/Math.cos(r))/Math.PI)/2*n*256;return [x,y]}
async function hydrateSatellite(){for(const el of document.querySelectorAll('.sat-map')){if(el.dataset.ready)return;const r=DATA.find(x=>x.id===el.dataset.sat);if(!r?.track_geojson_file)continue;try{const gj=await fetch(asset(r.track_geojson_file)).then(x=>x.json());const pts=gj.features[0].geometry.coordinates.map(c=>[c[0],c[1]]),lons=pts.map(p=>p[0]),lats=pts.map(p=>p[1]);let z=(Math.max(...lons)-Math.min(...lons)>0.12||Math.max(...lats)-Math.min(...lats)>0.08)?12:13;let x1=lon2tile(Math.min(...lons),z),x2=lon2tile(Math.max(...lons),z),y1=lat2tile(Math.max(...lats),z),y2=lat2tile(Math.min(...lats),z);if((x2-x1+1)*(y2-y1+1)>18){z=12;x1=lon2tile(Math.min(...lons),z);x2=lon2tile(Math.max(...lons),z);y1=lat2tile(Math.max(...lats),z);y2=lat2tile(Math.min(...lats),z)}const layer=el.querySelector('.tile-layer'),svg=el.querySelector('.sat-track'),w=(x2-x1+1)*256,h=(y2-y1+1)*256;layer.innerHTML='';layer.style.width=w+'px';layer.style.height=h+'px';svg.setAttribute('viewBox',`0 0 ${w} ${h}`);svg.style.width=w+'px';svg.style.height=h+'px';const scale=Math.min(el.clientWidth/w,el.clientHeight/h)*0.98;const tx='translate(-50%,-50%) scale('+scale+')';layer.style.transform=tx;svg.style.transform=tx;for(let x=x1;x<=x2;x++)for(let y=y1;y<=y2;y++){const img=document.createElement('img');img.src=`https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/${z}/${y}/${x}`;img.className='sat-tile';img.alt='';img.style.left=((x-x1)*256)+'px';img.style.top=((y-y1)*256)+'px';layer.appendChild(img);}const px=pts.map(([lon,lat])=>{const [x,y]=project(lon,lat,z);return `${x-x1*256},${y-y1*256}`}).join(' ');svg.querySelector('polyline').setAttribute('points',px);el.dataset.ready='1'}catch(e){}}}
function loadLogs(){try{LOGS=JSON.parse(localStorage.getItem('mtbLogs')||'[]');FAVS=JSON.parse(localStorage.getItem('mtbFavs')||'[]')}catch(e){LOGS=[];FAVS=[]}renderLogs()}function saveFavs(){localStorage.setItem('mtbFavs',JSON.stringify(FAVS))}function toggleFav(id){FAVS=FAVS.includes(id)?FAVS.filter(x=>x!==id):[...FAVS,id];saveFavs();render()}
function renderLogs(){const routes=Object.fromEntries(DATA.map(r=>[r.id,r.name]));$('logs').innerHTML=LOGS.slice().reverse().map(l=>`<div class="log"><b>${esc(l.date)} · ${esc(routes[l.route]||l.route)}</b><span>${esc(l.flow)} · ${esc(l.mud)}</span><p>${esc(l.note)}</p></div>`).join('')||'<p class="muted">Noch keine Fahrt gespeichert.</p>'}
$('logForm').addEventListener('submit',e=>{e.preventDefault();LOGS.push({route:$('logRoute').value,date:$('logDate').value||today(),flow:$('logFlow').value,mud:$('logMud').value,note:$('logNote').value.trim(),saved_at:new Date().toISOString()});localStorage.setItem('mtbLogs',JSON.stringify(LOGS));$('logNote').value='';renderLogs()});$('exportLogs').addEventListener('click',()=>{const blob=new Blob([JSON.stringify(LOGS,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='mtb-fahrtenlogbuch.json';a.click();URL.revokeObjectURL(a.href)});
document.addEventListener('click',e=>{const fav=e.target.closest('[data-fav]');if(fav){e.preventDefault();toggleFav(fav.dataset.fav);return}const f=e.target.closest('[data-focus]');if(f){FOCUS=f.dataset.focus;render();document.getElementById('routes').scrollIntoView({behavior:'smooth'});}});
[q,sort,type,timeBudget,batteryLeft,assist].forEach(el=>el.addEventListener('input',render));fetch(asset('routes.json')).then(r=>r.json()).then(d=>{MODEL=d;DATA=d.routes;populate();loadLogs();FOCUS=DATA.slice().sort((a,b)=>b.score-a.score)[0]?.id;render();loadWeather()});
