import { element,clear } from './dom.js';
import { estimateBattery,estimateTime } from './battery.js';
const minutes=value=>`${Math.floor(value/60)}:${String(value%60).padStart(2,'0')} h`;
export function renderRail(container,routes,focusId,favorites){clear(container);if(!routes.length){container.append(element('p',{className:'empty',text:'Keine passenden Bestandskandidaten.'}));return}for(const route of routes){const button=element('button',{className:`route-row${route.id===focusId?' is-active':''}`,attrs:{type:'button','data-route-id':route.id,'aria-label':`${route.name} öffnen`}});const copy=element('span');copy.append(element('strong',{text:route.name}),element('small',{text:`${route.distanceKm.toFixed(1)} km · ${route.elevationM} hm · ${route.difficulty}`}));button.append(copy,element('span',{className:'route-score',text:`${favorites.has(route.id)?'★ ':''}${route.score.toFixed(0)}`}));container.append(button)}}
export function renderFocus(container,route,{batteryWh,systemMassKg,isFavorite}){clear(container);if(!route){container.append(element('div',{className:'empty-state',text:'Noch keine fachlich freigegebene Empfehlung. Öffne die Kandidatenansicht, um den Bestand zu prüfen.'}));return}const header=element('div',{className:'focus-head'});const title=element('div');title.append(element('p',{className:'eyebrow',text:'Ungeprüfter Bestandskandidat'}),element('h2',{text:route.name}),element('p',{className:'focus-lead',text:`${route.region} · ${route.bestFor}`}));const fav=element('button',{className:'favorite',text:isFavorite?'★ Favorit':'☆ Favorit',attrs:{type:'button','data-route-id':route.id,'aria-label':'Favorit umschalten'}});fav.dataset.action='favorite';header.append(title,fav);const map=element('div',{className:'track-stage',attrs:{role:'img','aria-label':'Öffentlicher Track nicht verfügbar'}});map.append(element('div',{className:'map-contours'}),element('p',{text:'Track gesperrt'}),element('strong',{text:'Kein öffentlich freigegebener Startpunkt'}),element('small',{text:route.presentation.reason}));const metrics=element('div',{className:'metrics'});for(const [label,value] of [['Gesamtdistanz',`${route.distanceKm.toFixed(1)} km`],['Höhenmeter',`${route.elevationM} hm`],['Zeit Bestand',`${minutes(route.durationMinutes[0])}–${minutes(route.durationMinutes[1])}`],['Oberfläche',route.surface.join(' · ')]]){const item=element('div');item.append(element('span',{text:label}),element('strong',{text:value}));metrics.append(item)}const battery=estimateBattery({...route,batteryWh,systemMassKg}),time=estimateTime(route);const planner=element('section',{className:'planner'});planner.append(element('h3',{text:'Akku- & Zeitmodell'}),element('p',{text:battery?`${battery.requiredWh} Wh benötigt · ${battery.usableWh} Wh mit 20 % Reserve · ${battery.sufficient?'innerhalb Modellreserve':'nicht ausreichend'}`:'Werte außerhalb 250–1000 Wh oder 70–160 kg – nicht berechenbar.'}),element('p',{text:time?`Modellzeit ${minutes(time.minMinutes)}–${minutes(time.maxMinutes)}; ohne Wetter- oder Fahrbarkeitsaussage.`:'Zeit nicht berechenbar.'}));const details=element('details',{className:'details'});details.append(element('summary',{text:'Details & Prüfhinweise'}));const body=element('div');body.append(element('p',{text:`Saison: ${route.season}`}),element('p',{text:`Highlights: ${route.highlights.join(' · ')||'nicht dokumentiert'}`}),element('p',{text:`Prüfen: ${route.riskNotes.join(' · ')||'Vor realer Fahrt fachlich prüfen.'}`}));details.append(body);container.append(header,map,metrics,planner,details)}
